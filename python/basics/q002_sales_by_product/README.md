# q002_sales_by_product - 商品別売上集計

## 目的
商品ごとの売上合計を辞書でまとめ、売上の大きい順に表示する基礎を身につけることを目的としています。

## 使用技術
Python, csv, 辞書, for文, 関数, sorted

## 実行方法
1. `learning/main.py` を自力で完成させます。
2. `completed/main.py` と見比べて差分を確認します。
3. 必要なら `python learning/main.py` または `python completed/main.py` を実行します。
4. `docs/` を見返し、辞書での集計処理を説明できるようにします。

## 学び
- 辞書で集計する考え方を身につけると、カテゴリ別の集計処理に応用しやすくなります。

## 関連リンク
- [問題文](docs/problem.md)
- [要件](docs/requirements.md)
- [学習用コード](learning/main.py)
- [完成コード](completed/main.py)


## 2026年7月3日の学習記録

### 今週取り組んだ内容

今週は、SQLを4問、Python基礎を2問、Excelをq001〜q020まで学習しました。
今回はその中から、Python基礎の「商品ごとの売上合計を辞書にまとめる」課題について整理します。

### 目的

CSVファイルに記録されている商品名、数量、価格を読み込み、商品ごとの売上合計を辞書にまとめることを目的としました。

### 処理の流れ

1. CSVファイルを読み込む
2. 商品名、数量、価格を1行ずつ取り出す
3. 商品ごとに「数量 × 価格」を計算する
4. 計算した売上を商品ごとの辞書に加算する
5. 商品ごとの売上合計を、売上が大きい順に並び替えて表示する

### 今回学んだこと

今回の課題では、辞書にまとめた商品ごとの売上を並び替える方法を学びました。

`product_sales.items()` を使うことで、辞書のキーと値をセットで取り出せることが分かりました。

また、`sorted(product_sales.items(), key=lambda x: x[1], reverse=True)` と書くことで、辞書の値である売上合計を基準にして、売上が大きい順に並び替えられることを学びました。

特に、`lambda x: x[1]` は「商品名と売上合計のセットのうち、売上合計の部分を並び替えの基準にする」という意味だと理解しました。

### BtoB SaaSでどう活かせるか

商品ごとの売上を集計する考え方は、BtoB SaaSのプラン別売上、顧客別利用量、機能別利用回数などのKPI集計に活かせると感じました。
カテゴリごとにデータを集計して並び替える処理は、ダッシュボード作成やユーザー行動分析の基礎になると考えています。

