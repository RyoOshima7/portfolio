# dbt

## 目的
dbtでモデル管理やテストを行うための置き場を整理し、拡張しやすい構成を作ることを目的としています。

## 使用技術
dbt, SQL, YAML, Markdown

## 実行方法
1. 必要になったら `models/` `tests/` `macros/` を追加します。
2. モデルSQLとテスト定義を合わせて管理します。
3. 変換ルールが読み手に伝わる説明を付けます。

## 学び
- 変換処理をコード化して管理すると、再現性と保守性が高まります。
