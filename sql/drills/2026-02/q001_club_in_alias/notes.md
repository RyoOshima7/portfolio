## 要点（What）
- SELECT文でカラムに別名を付ける方法
- 同一カラムに複数条件を指定するケース

## 気づき・学び（Why）
- カラム名を分かりやすくする場合は `AS 別名` を使う
- 同一カラムに対する複数条件は `IN (...)` が簡潔（ORでも可）

## 詰まりポイント / 回避メモ（How）
- IN と OR の使い分けで一瞬迷った  
  → 「同一カラム × 複数値」は IN を優先して思い出す
