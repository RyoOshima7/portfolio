# 2026年5月9日 学習ログ

## 今日やったこと
- SQLの苦手問題を2問復習
- Pythonの苦手問題を2問再実行
- pandasでCSV読み込みの入口を作成
- Excel q011〜q020から1つ復習

## 分からなかったこと
- SQL
- Python
- Excel
- pandasの環境設定とCSV読み込みの流れ

## 今日の学び
- pandasを使うには、事前にインストールが必要であること
- `import pandas as pd` はpandasを使う準備であり、インストールそのものではないこと
- `pd.read_csv()` でCSVファイルをDataFrameとして読み込めること
- `head()`、`columns`、`info()`、`describe()` でデータの基本確認ができること
- `df.info()` はそれ自体で表示するため、`print(df.info())` と書くと最後に `None` が表示されること

## 次にやること
毎朝通知される学習内容をもとに、今まで学習した内容の復習と、就活ロードマップ達成のための新規学習を行う。
